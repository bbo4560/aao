<Window x:Class="TEST2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TEST2"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        Title="練習" Height="600" Width="1000" WindowStartupLocation="CenterScreen" Background="#F0F2F5" KeyDown="Window_PreviewKeyDown">

    <Window.Resources>
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="50"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Padding" Value="2"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Margin" Value="5,0,0,0"/>
            <Setter Property="Height" Value="30"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal">
                <Button x:Name="BtnAdd" Content="新增" Click="BtnAdd_Click" Width="80" ToolTip="快捷鍵：F1"/>
                <Button x:Name="BtnQuery" Content="查詢" Click="BtnQuery_Click" Width="80" ToolTip="快捷鍵：F2"/>

                <Button x:Name="BtnImport" Content="匯入" Click="BtnImport_Click" Width="80" ToolTip="快捷鍵：F3"/>
                <Button x:Name="BtnExport" Content="匯出" Click="BtnExport_Click" Width="80" ToolTip="快捷鍵：F4"/>

                <Button x:Name="BtnLog" Content="查看操作紀錄" Click="BtnLog_Click" Width="140" ToolTip="快捷鍵：F6"/>
                <Button x:Name="BtnBatchDelete" Content="批量刪除" Click="BtnBatchDelete_Click" Width="100" Background="#F44336" Foreground="White" Margin="20,0,0,0" ToolTip="快捷鍵：Delete"/>
                <Button x:Name="BtnShowAll" Content="顯示全部" Click="BtnShowAll_Click" Width="100" Background="#607D8B" Foreground="White" Margin="5,0,0,0" ToolTip="快捷鍵：F5"/>
            </StackPanel>

            <TextBlock x:Name="txtLastOpTime" Grid.Column="1" Text="最後操作時間: -" VerticalAlignment="Center" Foreground="#888888" FontSize="13"/>
        </Grid>

        <DataGrid x:Name="dataGrid" Grid.Row="1" AutoGenerateColumns="False" CanUserSortColumns="True" CanUserReorderColumns="False" IsReadOnly="True" PreviewKeyDown="DataGrid_PreviewKeyDown">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Time" Binding="{Binding Time, StringFormat='yyyy/MM/dd HH:mm:ss'}" SortMemberPath="Time" Width="200"/>
                <DataGridTextColumn Header="PanelID" Binding="{Binding PanelID}" SortMemberPath="PanelID" Width="*"/>
                <DataGridTextColumn Header="LOTID" Binding="{Binding LOTID}" SortMemberPath="LOTID" Width="*"/>
                <DataGridTextColumn Header="CarrierID" Binding="{Binding CarrierID}" SortMemberPath="CarrierID" Width="*"/>

                <DataGridTemplateColumn Header="操作" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="刪除" Click="BtnDelete_Click" Style="{StaticResource ActionButtonStyle}" Foreground="#FF5252"/>
                                <Button Content="修改" Click="BtnModify_Click" Style="{StaticResource ActionButtonStyle}" Foreground="#333333"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
